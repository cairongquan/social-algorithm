# 基于用户行为的社交媒体信息推送算法系统：答辩与演示指南

## 1. 论文怎么描述（3分钟版本）

### 1.1 研究问题
- 社交媒体信息量大、更新快，传统按时间流或统一分发方式难以满足个性化需求。
- 目标是构建一套可解释、可调参、可验证的用户行为驱动推荐系统。

### 1.2 核心方法
- 用户兴趣建模：基于浏览/点击/点赞/评论行为构建兴趣向量。
- 时间衰减：使用 `effective_weight = weight * decay_factor^days` 动态降低旧行为影响。
- 相似度计算：使用余弦相似度衡量用户兴趣与文章标签向量匹配程度。
- 综合排序：
  - `final_score = similarity_part + hot_part + follow_part + liked_part - penalty_part`
  - 支持基线模式切换（仅热度、仅相似度、相似度+热度）用于对比实验。

### 1.3 系统实现
- 后端：FastAPI + SQLite。
- 前端：Vue3 + TypeScript。
- 管理能力：
  - 管理员账户可查看全站文章。
  - 管理员可修改算法变量、恢复默认值。
  - 拓扑页仅管理员可见。
- 可解释性：广场每条推荐都有“为什么会刷到这篇文章”和分项得分展示。

### 1.4 实验结论（当前数据）
- `mode=0 full_model`：`recall@10=0.7188`，`ndcg@10=0.2448`
- `mode=1 hot_only`：`recall@10=0.0625`，`ndcg@10=0.0469`
- `mode=2 similarity_only`：`recall@10=0.2188`，`ndcg@10=0.0885`
- `mode=3 sim_hot`：`recall@10=0.2188`，`ndcg@10=0.0885`

结论：完整模型在当前实验集下明显优于基线策略。

---

## 2. 答辩常见问题与应对

### Q1：为什么不用深度学习模型？
- 当前研究目标是“可落地+可解释+可控”。
- 行为向量+衰减+余弦相似度实现成本低、解释性强，适合作为工程基线。
- 后续可在此框架上引入深度模型做增强对比。

### Q2：时间衰减有什么必要？
- 用户兴趣是动态变化的，旧行为长期等权会造成推荐滞后。
- 衰减机制能提升近期兴趣的响应速度，减少“过时兴趣”干扰。

### Q3：你怎么证明算法有效？
- 通过离线实验脚本统一评估 `Recall@K`、`NDCG@K`。
- 采用多模式对比（full_model vs baseline），展示量化提升。

### Q4：为什么要做可解释推荐？
- 提升系统透明度和用户信任。
- 便于排障和调参（可看到是相似度驱动还是热度驱动）。

### Q5：有没有冷启动问题？
- 有。当前通过热度因子、社交关系因子缓解。
- 后续可加入内容语义特征、用户注册问卷兴趣标签进一步优化。

### Q6：管理员调参会不会破坏稳定性？
- 有参数范围校验和权重和约束（四项权重和必须为1）。
- 提供“一键恢复默认值”降低误调风险。

---

## 3. 你答辩时可直接照说的话术（开场 30 秒）

“我的课题是基于用户行为分析的社交媒体信息推送算法设计与实现。核心思路是将用户浏览、点击、点赞、评论行为映射为兴趣向量，并结合时间衰减机制动态更新兴趣权重，再通过余弦相似度和热度、社交关系等因素进行综合排序。系统支持可解释推荐展示和管理员可视化调参，并通过离线对比实验验证完整模型优于基线策略。”

---

## 4. 风险点提醒（答辩前）
- 确认使用管理员账号 `admin/admin` 登录。
- 确认前端打开的是 `http://localhost:5173`。
- 确认后端端口是 `8002`。
- 演示前先刷新一次，避免缓存导致旧页面。
