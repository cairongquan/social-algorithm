# 现场演示脚本（按此顺序操作）

## A. 演示前准备（1分钟）
1. 打开前端：`http://localhost:5173`
2. 确认后端在线：`http://localhost:8002/api/v1/docs`
3. 使用管理员登录：
   - 用户名：`admin`
   - 密码：`admin`

可选：如果服务没开，执行
```bash
# 后端
cd service
./.venv/Scripts/python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8002

# 前端（新终端）
cd app
npm run dev -- --port 5173 --strictPort
```

---

## B. 演示流程（建议 8~10 分钟）

## 第1步：系统整体说明（30秒）
- 进入首页，说明系统模块：文章管理、广场推荐、标签管理、算法配置、拓扑分析。
- 强调“管理员可以调算法变量，并即时影响推荐排序”。

## 第2步：展示管理员能力（40秒）
- 顶部导航展示：`算法配置`、`拓扑`（仅管理员可见）。
- 说明普通用户看不到这两个入口。

## 第3步：展示算法配置页面（2分钟）
- 进入 `算法配置` 页面。
- 指出每个变量都有：
  - 含义解释
  - 对应公式
  - 建议范围
- 强调约束：四项权重和必须为 `1`。
- 演示一次“保存算法变量”。
- 演示一次“恢复默认值”。

推荐演示参数（轻微改动即可）：
- `similarity_weight`: `0.60 -> 0.50`
- `hot_weight`: `0.20 -> 0.30`
- `follow_weight`: `0.15`（不变）
- `liked_weight`: `0.05`（不变）

## 第4步：展示广场可解释推荐（2分钟）
- 打开“广场”页面。
- 展示每条内容下方：
  - “我为什么会刷到这篇文章”
  - 推荐分
  - 命中标签
  - 算法分解项
- 说明这实现了推荐可解释性和可审计性。

## 第5步：展示标签驱动推送预览（1.5分钟）
- 进入“创建文章”页面。
- 选择标签后，展示“可能会推送的用户”：头像、昵称、推送纪律、命中标签。
- 说明系统在发布前就可以预估触达人群。

## 第6步：展示我的文章/全站文章与评论管理（1.5分钟）
- 进入文章列表页。
- 管理员可看到“全部用户文章”。
- 展示评论常驻显示、发布评论、删除评论。
- 展示每篇文章的热度、时间衰减、推荐分解数据。

## 第7步：展示实验结果（1分钟）
- 口述离线评估结果（可贴在PPT）：
  - full_model：`recall@10=0.7188`，`ndcg@10=0.2448`
  - hot_only：`recall@10=0.0625`，`ndcg@10=0.0469`
  - similarity_only：`recall@10=0.2188`，`ndcg@10=0.0885`
- 结论：完整模型显著优于基线。

---

## C. 评估命令（答辩备用）

在 `service` 目录执行：
```bash
./.venv/Scripts/python.exe scripts/evaluate_recommendation.py --mode 0 --k 10 --output reports/eval_mode_0.csv
./.venv/Scripts/python.exe scripts/evaluate_recommendation.py --mode 1 --k 10 --output reports/eval_mode_1.csv
./.venv/Scripts/python.exe scripts/evaluate_recommendation.py --mode 2 --k 10 --output reports/eval_mode_2.csv
./.venv/Scripts/python.exe scripts/evaluate_recommendation.py --mode 3 --k 10 --output reports/eval_mode_3.csv
```

---

## D. 常见突发情况应对
- 页面没变化：强刷 `Ctrl+F5`。
- 看不到管理员入口：重新登录 `admin/admin`。
- 保存算法失败：检查四项权重和是否等于 `1`。
- 推荐结果异常：先点“恢复默认值”，再刷新广场。
